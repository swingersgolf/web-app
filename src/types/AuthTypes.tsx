export interface UserType {
    name: string;
    email: string;
    password: string;
    birthdate: string;
}

export interface ProfileType {
    handicap?: number;
    postalCode?: string;
}
